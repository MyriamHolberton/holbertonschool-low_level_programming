#include <stdlib.h>
#include<time.h>
#include<stdio.h>
/**
 * main-This is the main of my program
 * Return: Always 0
**/
int main(void)
{
char lit;
for (lit = 'z'; lit >= 'a'; lit--)
{
	putchar(lit);
}
{
	putchar(10);
}
	return (0);
	}
