#include <stdlib.h>
#include<time.h>
#include<stdio.h>
/**
 * main-This is the main of my program
 * Return: Anything that respects the syntax
**/
int main(void)
{
for (ch = 'lower' ; ch <= 'Z' ; ch++)
{
	putchar(ch);
}
return (0);
}
