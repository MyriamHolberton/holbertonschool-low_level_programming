#include <stdlib.h>
#include<time.h>
#include<stdio.h>
/**
 * main-This is the main of my program
 * Return: Anything that respects the syntax
**/
int main(void)
{
char lit;
for (lit = 'a'; lit <= 'z'; lit++)
{
	putchar(lit);
}
	putchar(10);
	return (0);
}
