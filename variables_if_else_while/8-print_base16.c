#include <stdlib.h>
#include<time.h>
#include<stdio.h>
/**
 * main-This is the main of my program
 * Return: Always 0.
**/
int main(void)
{
int b;
char lit;

for (b = '0'; b <= '9'; b++)
{
	putchar(b);
}
for (lit = 'a'; lit <= 'f'; lit++)
{
	putchar(lit);
}
{
	putchar('\n');
}
	return (0);

}
